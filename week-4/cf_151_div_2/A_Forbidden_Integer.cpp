#include<bits/stdc++.h>
#define fastread() {ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);}
#define ll long long
using namespace std;

void solve(){
    int n,k,x;  cin>>n>>k>>x;
    vector<int>v;
    for(int i=1;i<=n;i++){
        if(i==x)continue;
        v.push_back(i);
    }
    
}
int main(){
    fastread()
    int t;  cin>>t;
    while(t--)solve();
    return 0;
}