#include<bits/stdc++.h>
#define ll long long
using namespace std;

void solve(){
    // ll n, x, y;cin >> n >> x >> y;
    // ll l = x * y / __gcd(x, y);
    // x = n/x-n/l;
    // y = n/y-n/l;
    // ll a = (1 + y) * y / 2;
    // ll b = (n + n - x + 1) * x / 2;
    // cout <<a-b <<endl;
    
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    int t=1;  cin>>t;
    while(t--)solve();
    return 0;
}