#include<bits/stdc++.h>
#define fastread() {ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);}
#define ll long long
using namespace std;

void solve(){
    int n;    cin>>n;
    vector<int>a(n);
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    if(n==1)cout<<a[0]<<endl;
    else {
        int j=n;
        int d = abs(a[0]-a[1]);
        while(j>0 ){
            
        }
        if(!a.empty()){
            for(int i=n-j;i>=0;i--){

            }
        }
    }
}
int main(){
    fastread()
    int t;  cin>>t;
    while(t--)solve();
    return 0;
}